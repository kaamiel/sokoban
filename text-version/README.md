# Sokoban text version

```sh
ghc Main.hs
./Main
```

* control the player with the arrow keys,
* `q` – quit,
* `r` – reset,
* `u` – undo,
* `n` – next level,
* text representation of the board:
    * player: `'@'`,
    * wall: `'#'`,
    * ground: `' '`,
    * storage: `'.'`,
    * box: `'$'`
